# Local test configuration for context ladder
# Used when running from clawdbot (not CT327/llama-cpp)

LLAMA_ENDPOINT=http://192.168.0.27:8080/v1/completions
LLAMA_SSH_HOST=llama-cpp
MODEL_NAME=Qwen3.5-27B-UD-Q5_K_XL
OUTPUT_DIR=/var/lib/clawdbot/workspace/agents/labmaster/llmlab/scripts/results

# Set to "local" if running on the same machine as llama-server
# Set to "ssh" if running remotely
MONITOR_MODE=ssh
