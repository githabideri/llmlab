# Reports

Date-stamped findings — investigations, deployments, benchmarks, hardware forensics, and infrastructure notes. Each file is a **snapshot of what we knew at the time**; no future maintenance needed. If something changes, write a new report rather than editing an old one.

> **Format note:** these are *not* homelab reports. There is **no YAML frontmatter** and **no `<type>_` filename prefix** — naming is just `YYYY-MM-DD-<kebab-slug>.md`. (The private homelab repo uses a different, frontmatter-based convention; its `report` skill is explicitly not to be applied here.)

## Categories

- **Model deployment** — configs, sweeps, and performance numbers for a specific model + hardware combo
- **Experiment / benchmark** — a single test run: *Goal → Setup → Commands → Observations → Metrics → Conclusion*
- **Hardware** — PCIe forensics, BIOS gotchas, thermal / bandwidth analysis
- **Infrastructure** — serving setups, container configs, multi-machine ops

The directory is flat and date-sorted. Use the per-run structure above for experiments/benchmarks; deployments and forensics are free-form.

## Recent

- `2026-09-23-flash-next-pinned-path-oom-unpinned-ub-sweep.md` — why the pinned ubatch matrix (`GGML_CUDA_REGISTER_HOST=1`) **can't run on the single-3060 backup box**: it page-locks mmap'd expert weights, so one ~9.6K prefill pins the CT cgroup flat at ~40 GB against a 40 GiB soft limit on a 46 GiB no-swap host → three hard reboots (source-confirmed: `cudaHostRegister(Portable|ReadOnly)` in `ggml-cuda.cu`) · unpinned quick test: prod ub 512 does **45.8 t/s** cold prefill @9.6K and **beats** ub 1024 (26.1 t/s) on this host — larger upload batches stall the i3 pipeline, the opposite of the 08-28 `--no-mmap` result · decode 8.4–11.1 t/s below the warm baseline is page-cache state post-reboot, not config

- `2026-09-21-laya-doc-staleness.md` — Laya 421M (self-hosted, 2-core CPU) as a **documentation-staleness detector**: "is this doc stale?" is an abstract judgment (a measured 421M failure class), but **NLI on claim pairs against the fact-ownership map** works as a precision-first alarm — 3-way choice is the only usable shape (noul inverts on comparative questions in both polarities, 2-way is *worse*, an explicit subject prefix fixes labels but collapses confidence), and confidence cannot gate in this regime (correct answers arrive at 1–65%) · measured on the repo's own 2026-09 drift incidents: across 284 judgments **5 confident flags, all true, zero false** — the single live flag was a *genuine human miss* (the 35B index row still saying "multi-slot" after its dual-3060 home was dismantled 2026-09-08), and the just-scrubbed clean corpus flagged zero · pre-filter value (clearing the boring 97%) requires the fine-tune path (open) · method home: [docs/decision-classifiers](../docs/decision-classifiers.md)

- `2026-09-20-flash-next-single-3060-moe-cache-backup.md` — the MoE hot-cache recipe on the **single-3060 backup box** (i3-9100, 40 GB RAM): the 125B/6B Qwen4Exp at **~14.3–14.9 t/s decode** with a **64-slot** GPU hot-expert cache (+36% over no-cache on this box; the 5600X video reference does 24.4) · 9.6K-token prompts prefill at **48–53 t/s** (the 40-GB-RAM "full-speed" tier — no cliff) · **80-slot OOMs on long-prompt prefill** on a 12 GB card (the finding that set the 64-slot/64K ceiling) · MTP measured neutral (CPU-bound) · GPU 48 W in decode · adopted as an on-demand roster model behind the box's model-mux (35B stays resident)

- `2026-09-18-ista-3bit-27b-single-3060.md` — 3-bit (ISTA GSQ-RCO) Qwen3.8-27B on two single-3060 boxes: **64K + MTP is the ceiling** (25–29 t/s; only the D-CFR GDN-state patch makes 64K fit at 3-bit, and 67K first-token-OOMs in every batch/n-max/KV variant), **98K without MTP** on the stock build (16–20 t/s) · sampling regime measurably moves MTP acceptance (84% → 60% moving from Qwen thinking-mode defaults to ISTA's instruct values) · 3-bit passes a 5-item quality battery the 2-bit sibling fails (code + fact-check) → 2-bit retired · q8_0 K-cache is CPU-bound on this hybrid (7.6 t/s prefill — rejected) · shipped as the on-demand 27B behind each box's model-mux (8081)

- `2026-09-15-lmcache-ram-tier-vllm-dual3090.md` — LMCache 0.5.0 RAM-tier campaign on the production dual-3090 27B: **hard negative at engine init** — vLLM 0.28 requires `SupportsHMA` from any KV connector and LMCache 0.5.0 has no mamba/HMA code, so hybrid-SSM models die at KV-spec unification before the pool is ever touched (one step earlier than the 08-30 native-offload 0%-restore finding); the vLLM 0.28 connector contract documented along the way (`kv_connector_extra_config`, required `kv_role`, `lm://` remote URL, `expandable_segments`→cumem conflict); the science question (TTFT gain + restore safety) is untestable on this pair until LMCache ships HMA; control lanes measured real rows (40 K stores 30–69 s, APC-served returns ~1.5 s) — and the mixed-population wall-floor misfire became a methodology note in [docs/benchmarks.md](../docs/benchmarks.md)

- `2026-09-13-flash-next-single3090-codacus-cache.md` — Flash-Next Q2 **single-3090** Codacus hybrid baseline (the `thecodacus/llama.cpp` `perf` fork: GPU dense path + CPU experts + a GPU hot-expert cache + host PLE — deliberately *not* dependent on the dual-GPU tensor split): **~17–18 t/s** CPU-expert baseline (S0), **~26.5–27 t/s** with a 128-slot hot cache (+49–58%); the cache path is deterministic within a config but **not bit-identical to cache-off** under greedy decode (diverges from the first token, outputs stay coherent), so the strict bit-exact gate failed and **MTP was intentionally not tested**; 48 GiB host RAM (no swap) is a near-edge, *configurable* constraint (60 GiB available), making 128 slots more practical than 224

- `2026-09-11-multi-image-vision-ceiling.md` — research + campaign design for the **multi-image** ceiling on the 27B (written for external review): verified our model is `Qwen3VLProcessor` with **32×32 px/token** (patch 16×merge 2) and a **16 Mpx / 16,384-token default** that our 2 Mpx override cuts 8×; the 0.95 crash was an **encoder-activation → GDN/Mamba** OOM, not a count limit; the size-hints / `mm_processor_cache_gb` / `--mm-encoder-tp-mode` knob set (with #20123 / #27706 as the same failure mode elsewhere); a 3-axis campaign (count×pixel × gpu-util × encoder-tp) and a 7-goal decision matrix with the open questions we want feedback on

- `2026-09-11-dual3090-v2-operating-envelope.md` — operating-envelope campaign (cloud-model-orchestrated; box held all night — first unattended run after the two crashes, recovery stack on standby, zero intervention): **MTP knee at k=3** (67/113/151/168 t/s for k=0/2/3/4 on the 8192 profile — k4 only +11%, prod left untouched), **a 64K prefill in flight degrades running decode ~52%** (ITL 25.8→39.2 ms, full recovery after — the key 8192-vs-2048 input), **TP1 ≈ TP2** (149.8 vs 147.8 t/s at single-user load → one 3090 freeable for a second service), 32K prefix-cache 30× win (timing-sensitive); dev-phase field fixes (usage parsing, E2BIG, TP1 contract) logged by the orchestrator

- `2026-09-10-dual3090-overnight-campaign.md` — first unattended overnight run on the new 2×3090 box, orchestrated by a cloud model decoupled from the fleet: 27B profile fully attributed (MTP = 2.1×, eager = 3.4× penalty, 8192-batched wins at 16K → promoted to production), 640K soak (7 preemptions; 14–15 min TTFT per 43K-token prompt — the real cost of 262K), 128K ladder real / 256K a documented false-complete; Flash-Next 125B: load wall in the **tested build family** (25.75 GB unsplitable *layer* buffer > 24.37 GB card, `tensor` unimplemented, two builds) — a build limitation, not a proven capacity limit (45.9 GB non-PLE fits 48.6 GB aggregate; a working tensor split could still fit it); the host **hard-crashed mid-run with zero software trace** (second unexplained reboot in two days — PSU prime suspect) and the three-layer recovery design absorbed it with zero data loss
- `2026-09-07-llm-hub-adaptive-vision-routing.md` — four-day silent vision outage (static primary/fallback on a `--models-max 1` router; evicted model + slot-pinned fallback) → llm-hub advisor endpoint `GET /api/vision/route`: read-only, payload-free, two capacity classes (IMMEDIATE > QUEUED), filter-then-score, no backend mutation in Phase 1; measured fleet vision speeds (27B = 2× image tokens = worst route, not second-best); advisor-not-gateway rationale
- `2026-09-06-whisperx-pascal-dual-gpu-benchmark.md` — WhisperX large-v3-turbo (int8_float32) on 2× 2 GB Pascal (GTX 1050 + GT 1030, CC 6.1): ASR at 10.7× RT, full pipeline 7.1× RT; GPU placement proven per-stage; X-vector diarization replaces pyannote (322× faster, 31 MiB vs 1.5 GB)
- `2026-09-02-qwen4exp-flash-next-three-gpu-campaign.md` — 125 B Qwen4Exp MoE (51 B PLE table) on 3060/3060/3090: the master-build fitter's auto expert-spill beats every manual placement knob (30.2 t/s decode, ~390 prefill, +17 % MTP, ~1 Wh/1K tokens); last-`--override-tensor`-wins and fitter-disabling pitfalls; n-gram is code-toxic
- `2026-08-30-dual-3060-35b-squeeze-27b-node.md` — 2-bit 35B proven resident on one 12 GB 3060 (PCIe counters, 43–81 t/s); 27B dense on the 3060 pair at ~80% of 3090 speed for the same wall draw, with the ctx-degradation curve, concurrency cliff, and the cache-artifact retraction
- `2026-08-30-vllm-cpu-kv-offload-hybrid-mamba-fails.md` — vLLM native CPU KV offload writes but never restores on hybrid SSM/GDN models (0% hit rate, pool shrank); scheduler-side blocker, reverted
- `2026-08-28-llama-cpp-ubatch-moe-single-gpu.md` — `-ub 2048` gives +22% prompt-processing for MoE+MTP on a single 12 GB GPU, tg-neutral
- `2026-08-27-qwen3.6-35b-a3b-dual-3060-optimization.md` — two 3060s, 50/50 tensor split, 100+ t/s
- `2026-08-12-ds4-rocm-gfx1103-build-success.md` — DS4 ROCm gfx1103 build success
- `2026-07-24-35b-mtp-laptop-setup.md` — Qwen3.6-35B at 28 tok/s on a laptop (MTP + Vulkan iGPU)
- `2026-06-30-qwen3.6-35b-a3b-mtp-single-3060.md` — MoE offload mechanics, n-cpu-moe sweep, config for single RTX 3060 12GB

## Highlights

- `2026-03-14-ik-llama-cpp-graph-mode-multi-gpu.md` — graph mode: 3–4× speedup, VRAM leak fix, PCIe crash root cause on non-P2P topology
- `2026-03-14-qwen3.5-35b-a3b-vllm-pp3-concurrency.md` — vLLM PP=3 deployment + concurrency benchmarks on 3× RTX 3060
- `2026-03-09-concurrent-slot-asymmetry-investigation.md` — llama.cpp concurrent slot starvation (layer mode)
- `2026-06-19-beellama-dflash-cutover.md` — BeeLlama DFlash cutover (now historic; see the Qwen3.8-27B report)
